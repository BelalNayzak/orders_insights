import 'package:orders_insights/core/src/app_exports.dart';

class WSpace extends SizedBox {
  const WSpace(double size, {super.key}) : super(width: size);
}

class HSpace extends SizedBox {
  const HSpace(double size, {super.key}) : super(height: size);
}
